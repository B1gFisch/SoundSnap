export type Sound = {
    id: string;
    title: string;
    description?: string;
    audio_url: string;
    duration?: number;
    color?: string;
    created_date?: string;
    favorite?: boolean;
};
